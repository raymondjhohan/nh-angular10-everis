import { BookTopExampleDirective } from './book-top-example.directive';

describe('BookTopExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new BookTopExampleDirective();
    expect(directive).toBeTruthy();
  });
});
